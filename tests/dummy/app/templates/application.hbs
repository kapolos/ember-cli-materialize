<div class="navbar-fixed">
{{#md-nav as |n|}}
  {{#n.title 'index'}}
    ember-cli-materialize {{/n.title}}
  {{#n.menu class='right' as |m|}}
    {{#m.btn text='Components' as |b|}}
      {{#b.dropdown as |d|}}
        {{d.link-to 'Forms' 'forms'}}
      {{/b.dropdown}}
    {{/m.btn}}
  {{/n.menu}}
{{/md-nav}}
</div>

<div class="container">
  <div class="row">
    <div class="col s12">
      {{outlet}}
    </div>
  </div>
</div>



